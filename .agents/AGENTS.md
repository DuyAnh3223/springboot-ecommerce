# `.agents` Documentation Scope

- Root project instructions are in `../AGENTS.md`.
- Keep reusable rules in `.agents/rules/` and domain-specific references in `.agents/skills/`.
- Do not duplicate or contradict root or scope-local `AGENTS.md` files here.

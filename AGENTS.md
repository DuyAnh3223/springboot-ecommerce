# ABTechZone Repository Instructions

- This repository contains a Next.js client in `client/` and a Spring Boot backend in `server/`.
- Follow the nearest `AGENTS.md` for the files being changed; scope-specific instructions override this file.
- Plan and persist a walkthrough for non-trivial implementation or architecture changes. Trivial local edits do not require those documents.
- Canonical project documentation lives in `.agents/docs/plans/` and `.agents/docs/walkthroughs/`.
- Read only the rules and skills relevant to the task; do not load the entire `.agents/` tree.
- Exclude generated/dependency directories such as `client/.next/`, `client/node_modules/`, and `server/target/` from source audits.
- Do not change behavior, contracts, or unrelated working-tree changes outside the requested scope.
